export type Drop = {
  id: string;
  name: string;
  totalStock: number;
  description: string;
  price: number;
  availableStock: number;
  isActive: boolean;
  startsAt: string;
  createdAt: string;
  updatedAt: string | null;
};
